name: Setup and install
description: Common setup steps for CI — install Bun and project dependencies

runs:
  using: composite
  steps:
    - uses: oven-sh/setup-bun@v2
      with:
        bun-version: 1.3

    - name: Install dependencies
      shell: bash
      run: bun install --frozen-lockfile
