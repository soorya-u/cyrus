import {
	account,
	accountRelations,
	deviceCode,
	session,
	sessionRelations,
	user,
	userRelations,
	verification,
} from "./auth";

export const models = {
	account,
	accountRelations,
	deviceCode,
	session,
	sessionRelations,
	user,
	userRelations,
	verification,
};
