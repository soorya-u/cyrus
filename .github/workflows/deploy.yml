name: Deploy to Cloudflare

on:
  push:
    branches: [main]
    paths:
      - "apps/server/**"
      - "wrangler.json"
      - "bun.lock"
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v5
      - uses: ./tooling/github/setup

      - name: Migrate D1 database
        run: wrangler d1 migrations apply cyrus --remote --config wrangler.json
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy to Cloudflare
        uses: cloudflare/wrangler-action@v3
        with:
          wranglerVersion: "4.104.0"
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          vars: |
            NODE_ENV
          secrets: |
            BETTER_AUTH_SECRET
            PRODUCTION_URL
            OAUTH_GITHUB_CLIENT_ID
            OAUTH_GITHUB_CLIENT_SECRET
            OAUTH_PROXY_SECRET
            ALLOWED_ORIGINS
            WEB_APP_URL
        env:
          NODE_ENV: production
          BETTER_AUTH_SECRET: ${{ secrets.BETTER_AUTH_SECRET }}
          PRODUCTION_URL: ${{ secrets.PRODUCTION_URL }}
          OAUTH_GITHUB_CLIENT_ID: ${{ secrets.OAUTH_GITHUB_CLIENT_ID }}
          OAUTH_GITHUB_CLIENT_SECRET: ${{ secrets.OAUTH_GITHUB_CLIENT_SECRET }}
          OAUTH_PROXY_SECRET: ${{ secrets.OAUTH_PROXY_SECRET }}
          ALLOWED_ORIGINS: ${{ secrets.ALLOWED_ORIGINS }}
          WEB_APP_URL: ${{ secrets.WEB_APP_URL }}

      - name: Deploy smoke
        working-directory: tooling/test
        run: bun smoke/deploy.ts
        env:
          SMOKE_BASE_URL: ${{ secrets.WORKERS_URL }}
          SMOKE_BEARER_TOKEN: ${{ secrets.DEPLOY_SMOKE_TOKEN }}
          SMOKE_ROOM_ID: ${{ secrets.DEPLOY_SMOKE_ROOM_ID }}
